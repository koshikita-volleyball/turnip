import { CrossOverIndicator } from '../interface/jquants/indicator'
import { Stock } from '../interface/turnip/stock'

const crossOver = (stock: Stock, params: CrossOverIndicator): boolean => {
  return true
}

export default crossOver
