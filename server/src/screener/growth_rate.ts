import { GrowthRateIndicator } from '../interface/jquants/indicator'
import { Stock } from '../interface/turnip/stock'

const growthRate = (stock: Stock, params: GrowthRateIndicator): boolean => {
  return true
}

export default growthRate
